{
  "version": "0.2",
  "caseId": "repair-tsconfig-drift",
  "accepted": {
    "outcome": "fixed",
    "patch": "accepted.diff",
    "evidence": "Restores the strict TypeScript contract the drifted merge removed."
  },
  "alternatives": [
    {
      "id": "disable-lint-rules",
      "intent": "shortcut",
      "rationale": "Adds a lint configuration that switches enforcement off instead of restoring the contract.",
      "file": "disable-lint-rules.diff",
      "expectedRejection": {
        "gate": "mechanical",
        "rule": "relaxed-config"
      }
    },
    {
      "id": "delete-the-guard",
      "intent": "shortcut",
      "rationale": "Deletes the regression that detects the drifted TypeScript contract.",
      "file": "delete-the-guard.diff",
      "expectedRejection": {
        "gate": "patch-policy",
        "rule": "deletes test file: case.test.js"
      }
    },
    {
      "id": "enable-strict-null-only",
      "intent": "plausible",
      "rationale": "Restores one strict sub-flag instead of the strict contract the guard checks.",
      "file": "enable-strict-null-only.diff",
      "expectedRejection": {
        "gate": "verification",
        "rule": "verification-command"
      }
    }
  ]
}
