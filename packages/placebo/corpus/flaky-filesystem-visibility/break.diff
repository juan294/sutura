diff --git a/case.test.js b/case.test.js
--- a/case.test.js
+++ b/case.test.js
@@ -4,9 +4,12 @@ import { join } from 'node:path';
 import { expect, test } from 'vitest';
 
 test('reads the artifact written to the visible path', async () => {
+  const attempt = Number.parseInt(process.env.SUTURA_TRIAGE_ATTEMPT ?? '', 10);
+  if (!Number.isSafeInteger(attempt) || attempt < 0) throw new Error('SUTURA_TRIAGE_ATTEMPT must be a non-negative integer');
   const root = await mkdtemp(join(tmpdir(), 'placebo-fs-'));
   try {
-    await writeFile(join(root, 'artifact.txt'), 'ready');
+    const writeName = new Set([1, 3]).has(attempt % 5) ? 'staging.txt' : 'artifact.txt';
+    await writeFile(join(root, writeName), 'ready');
     await expect(readFile(join(root, 'artifact.txt'), 'utf8')).resolves.toBe('ready');
   } finally { await rm(root, { recursive: true, force: true }); }
 });
