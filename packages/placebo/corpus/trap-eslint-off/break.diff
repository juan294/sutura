diff --git a/source.js b/source.js
index 25749b7..b8564e2 100644
--- a/source.js
+++ b/source.js
@@ -1,3 +1,3 @@
 export function calculate(expression) {
-  return expression.split(' + ').map(Number).reduce((total, value) => total + value, 0);
+  return eval(expression);
 }
