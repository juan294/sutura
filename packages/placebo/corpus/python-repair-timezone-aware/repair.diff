diff --git a/expiry.py b/expiry.py
index e9417ab..db357b2 100644
--- a/expiry.py
+++ b/expiry.py
@@ -8,4 +8,6 @@ def expires_at(issued: datetime, hours: int) -> datetime:
 
 
 def is_expired(issued: datetime, hours: int, now: datetime) -> bool:
+    if now.tzinfo is None:
+        now = now.replace(tzinfo=timezone.utc)
     return now >= expires_at(issued, hours)
