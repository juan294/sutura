diff --git a/lookup.ts b/lookup.ts
index 7e8c05a..ebce4a5 100644
--- a/lookup.ts
+++ b/lookup.ts
@@ -1,3 +1,3 @@
 export function label(names: Map<string, string>, id: string): string {
-  return names.get(id) ?? "unknown";
+  return names.get(id);
 }
