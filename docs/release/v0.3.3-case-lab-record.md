# v0.3.3 Case Lab record

Date: 2026-09-24

Status: v0.3.3 released and published; the Case Lab is bound to v0.3.3 and
deployed with live runs enabled; the required smoke run published a live
`fixed` result with the GPT-6 Astra and TypeSafe Jev audit rows.

The fourth cycle through `### 8a. Case Lab follows the release`
(`docs/release/e2e-pro-playbook.md`). The release carries the #151 fix
(diagnose the earliest failed step), the release-gate fixes found in the
v0.3.2 cycle, and the benchmark workflow change that passes the optional audit
keys.

## Release (Part A)

- Version literals bumped on `develop` in `0fd0089`
- First squash `d545a53` (PR [#155](https://github.com/juan294/sutura/pull/155))
  failed `ci.yml` on `main` twice: the pre-push contract test runs the Case Lab
  gate, whose `git fetch --unshallow` refused `main`'s checkout as already
  complete. Not tagged. Fixed on `develop` in `9caecda` (accept an
  already-complete checkout, with a test) and re-cut
- Release squash `43fc66bc6dcd442a590ce9279099e925fb3f7951` (PR
  [#156](https://github.com/juan294/sutura/pull/156)); `ci.yml` on `main`
  green (run 35904939481) before tagging
- Tag `v0.3.3` and GitHub release
  [v0.3.3](https://github.com/juan294/sutura/releases/tag/v0.3.3)
- npm `sutura@0.3.3` published by `publish.yml` run 35907176221, which passed
  on the first attempt: its new wait step removed the verification race of
  v0.3.1 and v0.3.2
- `develop` reconciled with `git merge -s ours origin/main` after each squash

## Benchmark (Part B)

- Candidate = controller = subject: `43fc66bc6dcd442a590ce9279099e925fb3f7951` (v0.3.3)
- Manifest `docs/demo/run-manifests/release-v0.3.3-benchmark.json`, cap USD 15,
  reserve USD 1.00 (raised from 10 because both optional voices were expected
  to run)
- Canary at the tag: [provider-contract-canary run 35907176485](https://github.com/juan294/sutura/actions/runs/35907176485)
- 51/51 cases, 55/55 evaluations, zero false approvals, no infrastructure
  stops; 2 h 59 min wall clock
- Total USD 4.44947424 (inference USD 0.145514, sandbox USD 4.30396024)
- Trap catch 18/19, fix rate 12/18, flaky accuracy 10/10: unchanged from v0.3.2
- Evidence: `docs/demo/sutura-v0.3.3-release-benchmark-evidence.md`
- **Limitation:** the workflow step received both keys, but every audit recorded
  both optional voices as not configured. `healCase`, the local heal path the
  benchmark drives, never passed them to adjudication. The GitHub Action path
  does, so the live smoke below shows both rows. Fixed on `develop` after the
  release, with a regression test; the next release benchmark is the first to
  exercise both voices

## Publish and deploy (Part B)

- `release:case-lab bump --tag v0.3.3` rebound `release.json`, the demo
  workflow pins, `evidence.ts` and `replay.ts` (`34074f7`); recorded
  expectations rebound to the new costs and timings (no outcome changed)
- Controller pin set to the bind commit (`006a3a5`). The bind, pin and fix
  commits went up in one push, with no bypass: the local-first controller read
  added in the v0.3.2 cycle made that possible
- `publish-demo --authorize`: `sutura-demo` workflow byte-identical to the
  committed copy
- `deploy --authorize`: health reports
  `{"enabled":true,"release":{"version":"0.3.3","actionSha":"43fc66b…"}}`

## Smoke

- Dispatch `cl-1790203673831-fa626301` (demo run
  [35930290601](https://github.com/juan294/sutura-demo/actions/runs/35930290601)):
  published, `mode: live`, `outcome: fixed`, `matchesExpectation: true`,
  release 0.3.3; all eleven audit checks passed, including `second-opinion`
  and `typesafe-audit`; cost inference USD 0.060858, sandbox USD 0.19457677
- Result page: `https://sutura-case-lab.vercel.app/result/?id=cl-1790203673831-fa626301`

## Fleet

- Re-pin PRs to v0.3.3 opened on 16 consumer repositories (same set as
  v0.3.2: layalga, sutura-demo and roots excluded); all 16 merged. kalpha and
  home-network each failed one check that fails identically on the base
  branch; archy's contract tests passed on a rerun; coach's CI queued about
  six hours for runners before passing
- paisaxe #973's required Vercel preview smoke timed out: its `vercel.json`
  ignore step skips any non-production build without a pull-request id, and
  the branch commit was pushed before the PR existed. An empty commit on the
  open PR triggered the preview; the smoke passed and the PR merged. The
  re-pin script should open the PR before committing (or push after), so the
  first push already carries a PR id
- chapa #1337 is green after a full rerun (a failed-jobs-only rerun could not
  find the first attempt's build artifact); Juan merged it himself, as he
  is actively working in that repository

## Manual verification (Juan)

- Authorizations given in the session on 2026-09-23: version 0.3.3, benchmark
  cap USD 15, release and publish, fleet re-pin by PR per repository
