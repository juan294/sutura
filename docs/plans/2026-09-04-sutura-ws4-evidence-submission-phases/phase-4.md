# Phase 4: Phase 5 measured assembly

Status: Blocked on committed Phase 0 and WS-1/WS-2/WS-3 evidence

Issues: #103, #106, final #56 and #57

## Tasks

1. Read the committed benchmark, dogfood, Counterfactual/Arena, Data Lab,
   external-user, and release evidence in full.
2. Write the measured section last. Include the complete denominators, failed
   IDs or failure classes, inference-versus-sandbox cost labels, latency or
   operation measures, and exact public links. Never calculate from prose.
3. Add live-provider observations to the feedback report only when a public-safe
   run or hashed local evidence record exists.
4. Insert the public repository, Case Lab, release, Marketplace, benchmark,
   Arena, evidence index, and video URLs after each target is immutable and
   signed-out accessible.
5. Update the video script with the same evidence-bound numbers. Juan records,
   captions, and uploads the public video; WS-4 validates it in Phase 6.
6. Create candidate-bound `devpost` and `feedback` records under `docs/demo/`
   or `docs/feedback/`, including content hashes and the exact candidate.

## Verification

Run the submission contract, release-contract suite, evidence generator, link
checks, and manual claim-by-claim comparison against source artifacts. Close
#103, #106, #56, and #57 only after their complete public evidence is committed.
