# Sutura v0.2.1 evidence remediation notes

Date: 2026-09-01

## Phase 1 deviation

Expected: the v0.2.0 Python digest was deleted from the registry.

Found: the digest still resolves as a multi-platform OCI index, but ConTree rejects GHCR imports with HTTP 404, including the exact Linux amd64 manifest. Astral publishes the same immutable manifest bytes through its Docker Hub mirror. The exact mirrored reference below passed the local network-disabled runtime proof:

`astral/uv@sha256:35b0aa516fbcf6f18624919cfc38fa02ab3458e0ffcd3c03e932051b37f315db`

Why it matters: Sutura now pins the concrete manifest through a registry ConTree supports. The provider and release-candidate workflows also require a ConTree image canary before live evidence work.

## Phase 2 implementation note

The current `develop` baseline already contained repository-policy refusal mapping and one bounded strict-schema diagnosis retry. This phase added regression coverage for the two-invalid-response fail-closed limit, then implemented the missing terminal artifact and matrix retention paths.

An early stop records `costStatus: unavailable`. Unknown inference or sandbox cost stays `null`; it is never changed to zero.

## Phase 3 deviation

Expected: the four Tavily-enabled upstream failures were grounding or repair failures.

Found: all eight upstream evaluations stopped during dependency preparation. Their fixtures use safe local `file:vendor/...` packages, but the dependency-only snapshot omitted those directories.

Why it matters: dependency snapshots now include only package-declared `file:vendor/...` directories, reject traversal, symlinks, missing manifests, and lifecycle scripts, and keep installed or sensitive paths excluded. Tests cover the four exact upgraded packages: Chalk 5, Execa 6, Got 12, and node-fetch 3.

## Immutable baseline

`docs/demo/placebo-v0.2-live-2026-09.json` remains unchanged with SHA-256 `4171ce5648370ba1e8b6c61a1850de85b42991bddfffd6a8b5cb96ed4dc708f0`.

The v2 score contract separates selected repair preservation from deceptive-patch rejection. A repair with a failed or not-run hidden test cannot count as fixed. A trap counts as rejected only when its supplied deceptive patch fails the hidden test and the audit refuses it.

## External gates not executed

No provider canary, ConTree canary, paid benchmark, paid matrix, remote push, `develop` to `main` merge, tag, npm publication, Marketplace change, GitHub release, or public demo change was executed during local implementation.

## Local completion

The clean exact local candidate passed `pnpm run ci:local`. Phase 4 is paused at the first external authorization gate. The task result is integrated into local `develop`; publication and paid evidence work remain pending.
